version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: madutxo-liquid-electrs_web_1
      APP_PORT: 8080

  web:
    image: nginx:1.27-alpine
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/web/index.html:/tmp/index.template.html:ro
      - ${APP_DATA_DIR}/web/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ${APP_DATA_DIR}/icon.svg:/usr/share/nginx/html/icon.svg:ro
      - ${APP_DATA_DIR}/web/umbrel-qr-icon.svg:/usr/share/nginx/html/umbrel-qr-icon.svg:ro
    environment:
      ELECTRS_TOR_ADDRESS: "${APP_ELECTRS_LIQUID_RPC_HIDDEN_SERVICE}"
      ELECTRS_LIQUID_VERSION: "${APP_VERSION}"
    command: ["sh", "-c", "envsubst '$$ELECTRS_TOR_ADDRESS $$ELECTRS_LIQUID_VERSION' < /tmp/index.template.html > /usr/share/nginx/html/index.html && exec nginx -g 'daemon off;'"]

  api_cors:
    image: nginx:1.27-alpine
    restart: on-failure
    ports:
      - "9130:9130"
    command: ["sh", "-c", "echo 'server { listen 9130; location / { proxy_pass http://madutxo-liquid-electrs_electrs_1:3000; proxy_set_header Host $$host; add_header Cache-Control no-cache; add_header Access-Control-Allow-Origin * always; add_header Access-Control-Allow-Methods \"GET, OPTIONS\" always; add_header Access-Control-Allow-Headers Content-Type always; if ($$request_method = OPTIONS) { return 204; } } }' > /etc/nginx/conf.d/default.conf && exec nginx -g 'daemon off;'"]

  electrs:
    image: ghcr.io/madutxo/electrs-liquid:0.6.0@sha256:9d59a3d9ea85706227bc3b69c9a21c397bd9461d65eef867dd43ebb6b6cf1216
    restart: on-failure
    ports:
      - "60601:60601"
      - "3000:3000"
    stop_grace_period: 60s
    volumes:
      - "${APP_DATA_DIR}/data/electrs:/data"
    environment:
      ELEMENTS_RPC_PASSWORD: "${APP_ELECTRS_LIQUID_ELEMENTS_RPC_PASS}"
      # Increase daemon timeouts to tolerate Elements IBD load (default 10s/600s) and reduce reconnect spam
      DAEMON_CONNECTION_TIMEOUT: "15"
      DAEMON_READ_TIMEOUT: "600"
      DAEMON_WRITE_TIMEOUT: "600"
    command:
      - -vv
      - --network
      - liquid
      - --daemon-rpc-addr
      - "elements_node_1:7041"
      - --electrum-rpc-addr
      - "0.0.0.0:60601"
      - --http-addr
      - "0.0.0.0:3000"
      - --db-dir
      - /data/db
      - --db-block-cache-mb
      - "32"
      - --db-write-buffer-size-mb
      - "64"
      - --daemon-parallelism
      - "2"
      - --jsonrpc-import

  tor:
    image: getumbrel/tor:0.4.7.8@sha256:2ace83f22501f58857fa9b403009f595137fa2e7986c4fda79d82a8119072b6a
    user: "1000:1000"
    restart: on-failure
    volumes:
      - ${TOR_DATA_DIR}:/data
    environment:
      HOME: "/tmp"
    entrypoint: ["/bin/sh", "-c"]
    command: ["mkdir -p /data/app-madutxo-liquid-electrs/main && chmod 700 /data/app-madutxo-liquid-electrs/main && printf 'HiddenServiceDir /data/app-madutxo-liquid-electrs/main\\nHiddenServicePort 60601 madutxo-liquid-electrs_electrs_1:60601\\n' > /tmp/torrc && exec tor -f /tmp/torrc"]




