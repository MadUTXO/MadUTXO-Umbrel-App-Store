version: "3.7"

services:
  app_proxy:
    depends_on:
      - orchard
    environment:
      APP_HOST: madutxo-orchard_orchard_1
      APP_PORT: 3321

  orchard:
    image: ghcr.io/cashubtc/orchard-mintdb-${FLAVOR:-sqlite}:${VERSION:-latest}
    user: "1000:1000"
    init: true
    restart: on-failure
    stop_grace_period: 1m
    command: ["npm", "run", "start"]
    volumes:
      - ${APP_DATA_DIR}/data:/app/data
      - ${APP_DATA_DIR}/mint-data:/app/data/mint-data
    environment:
      TZ: ${TZ:-UTC}
      NODE_ENV: production
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: "3321"
      SETUP_KEY: ${APP_SEED:-orchardkey}
      DATABASE_DIR: /app/data
      MINT_DATANAME: mint.sqlite3
      MINT_DATADIR: /app/data/mint-data
      MINT_RPC_MTLS: "false"

volumes:
  data:
