version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: madutxo-headscale_main_1
      APP_PORT: 8080

  main:
    image: hwdsl2/headscale-server:latest
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/var/lib/headscale:rw
    environment:
      - TZ=${TZ:-UTC}
      - HS_SERVER_URL=${HS_SERVER_URL:-http://192.168.1.1:8080}
      - HS_LISTEN_PORT=8080
      - HS_METRICS_PORT=9090
      - HS_BASE_DOMAIN=headscale.internal
      - HS_USERNAME=admin
      - HS_DNS_SRV1=1.1.1.1
      - HS_DNS_SRV2=1.0.0.1
      - HS_LOG_LEVEL=error

  ui:
    image: ghcr.io/gurucomputing/headscale-ui:latest
    restart: on-failure
    environment:
      - TZ=${TZ:-UTC}
      - PORT=3000

  setup:
    build: ./docker
    container_name: madutxo-headscale_setup
    restart: on-failure
    ports:
      - "8084:8084"
    volumes:
      - ${APP_DATA_DIR}/data:/data:rw

volumes:
  data:
